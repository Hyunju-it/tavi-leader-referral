import { availableFonts } from '../utils/fonts';

export const devCommands = {
  dev: "Available developer commands: [fonts, font <fontname>]",
  fonts: `Available fonts: [${availableFonts.join(', ')}]`,
};
